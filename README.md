# Hola Mundo en Python

Este es un ejemplo simple de "Hola, mundo" en Python.

Para ejecutar el script desde la terminal:

```powershell
cd "c:\\Personal\\Projects\\Sample1"
python hola_mundo.py
```

Si `python` no está disponible, intenta usar `py -3` en Windows.
